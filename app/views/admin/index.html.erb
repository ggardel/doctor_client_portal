<h1>Admin#index</h1>
<p>Find me in app/views/admin/index.html.erb</p>

<ul>
    <% @patient_infos.each do |patient_info| %>
        <a href="/admin/<%= patient_info.user_id %>" >
        <li>
            <strong> <%= patient_info.name%></strong>
        </li>
</a>

    <% end %>
</ul>
<%= link_to "Add a new Patient", patient_infos_path %>
</br>
<%= link_to "Back", welcome_index_path %>
